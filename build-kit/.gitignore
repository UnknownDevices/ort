# Build outputs and caches — never commit compiled libs, ccache, or SDKs.
/out/*
!/out/.gitkeep
/cache/
*.log

# Vendored SDKs (e.g. the auth-gated TensorRT-RTX tarball for nv-trt-rtx).
/vendor/*
!/vendor/README.md
